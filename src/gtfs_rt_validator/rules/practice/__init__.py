"""The pinned GTFS-Realtime Best Practices, P-prefixed.

Every rule here cites the statement it enforces, as `practice: <clause>`, and
does not register without one. Roughly 62 normative statements is a measured
surface, not a rule list; the same per-clause decisions the `spec` tier needs
apply here.
"""

from __future__ import annotations
